{
  "name": "@collab-edit/scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript scripts for the collab-edit-demo project",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "test:ci": "vitest --run --coverage"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@vitest/coverage-v8": "^1.1.0",
    "typescript": "^5.7.2",
    "vitest": "^1.1.0"
  }
}
